# Simple TCP Server

This repository will create a simple tcp logging server. This server should receive tcp requests and save the request into a file. Each request will be saved on its own file with a unique id and the current day and time.

##Instructions

1. Type node server.js to launch server.
2. Type gulp as default for gulp lint and gulp mochaTest.
3. Type mocha to test serverTest.js
